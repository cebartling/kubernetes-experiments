apiVersion: v1
kind: Service
metadata:
  name: kubernetes-experiments-01-portal
spec:
  type: NodePort
  selector:
    app.kubernetes.io/instance: kubernetes-experiments-01-portal
  ports:
    - port: 3000
      targetPort: 3000
      nodePort: 32000


